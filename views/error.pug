<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title 오류
    body
        script const message = '#{message}'; const redirect = '#{redirect}'
        script.
            alert(message);
            window.location.href = redirect;
            